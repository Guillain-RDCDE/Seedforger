namespace Seedforger {

  /// <summary>UI language. Lives in the core so engine-side options and the
  /// (portable) string tables can reference it without a UI dependency.</summary>
  internal enum Language { English, French }
}
